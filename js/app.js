/* MostarModal al cargar la pagina */
const mostrarModalOferta = new bootstrap.Modal(
  document.getElementById('mostrarOferta')
)

/* const mostrarModal = ()=>{
  mostrarModalOferta.show()
}  */

document.addEventListener('DOMContentLoaded', mostrarModal);